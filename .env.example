# Local development defaults only. Set unique secrets for shared deployments.
FRONTEND_PORT=8080
MERCHANTS_DB_PASSWORD=merchants_local_only
CATALOG_DB_PASSWORD=catalog_local_only
ORDERS_DB_PASSWORD=orders_local_only
SERVICE_KEY=base-grid-local-service-key
